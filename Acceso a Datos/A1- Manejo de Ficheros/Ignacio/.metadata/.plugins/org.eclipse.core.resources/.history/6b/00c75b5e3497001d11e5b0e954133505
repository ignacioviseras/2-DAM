package dao;

import java.util.ArrayList;

import beans.Coche;

public class CocheDaoImpl implements IntCocheDao{

	private ArrayList<Coche> carList, aux;
	
	public CocheDaoImpl() {
		carList = new ArrayList<Coche>();
		aux = new ArrayList<Coche>();
	}
	
	/*necesito esto??
	 * creo q no pq los coches se cargan del fichero
	 */
	public void loadCar() {
		
	}
	
	/**
	 * Metodo para CREAR un coche nuevo
	 * @return 1 en caso de que EXISTA
	 *  <br> 
	 * 		 0 en caso de que SE CREE
	 */
	@Override
	public int newCar(Coche coche) {
		if(carList.contains(coche))
			return 1;
		return 0;
	}

	/**
	 * Metodo para BORRAR un coche
	 * @param id <li>ID del coche que se borrara
	 * @return True -> Se borro el coche
	 *  <br> 
	 * 		 False -> NO se borro el coche
	 */
	@Override
	public Boolean deleteCar(int id) {
		Coche cAux= new Coche();
		cAux.setId(id);
		int pos = carList.indexOf(cAux);
		if (pos == -1)//no existe
			return false;
		carList.remove(pos);//existe y se borrara
		return true;
	}

	/**
	 * Metodo para BUSCAR un coche
	 * @param id <li>ID del coche que se quiere buscar
	 */
	@Override
	public Coche findById(int id) {
		Coche aux= new Coche();
		aux.setId(id);
		int pos= carList.indexOf(aux);
		if (pos == -1)
			return null;
		else
			return carList.get(pos);
	}

	/**
	 * Metodo para CREAR un coche nuevo
	 * @return Devuelve la lista de coches
	 */
	@Override
	public ArrayList<Coche> listCar() {
		return carList;
	}

}
